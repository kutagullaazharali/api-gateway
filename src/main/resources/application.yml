server:
  port: 8081

spring:
  cloud:
    gateway:
      routes:
        - id: customer_service
          uri: http://localhost:8082
          predicates:
            - Path=/api/customers/** 
          filters:
            - StripPrefix=0
